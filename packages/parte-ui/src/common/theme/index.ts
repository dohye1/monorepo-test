import {
  COLORS,
  SPACING,
  TYPOGRAPHY,
  ELEVATION,
  commonStyles,
  colorTokens,
} from '@template/foundation';

const theme = {
  colors: COLORS,
  spacing: SPACING,
  typography: TYPOGRAPHY,
  elevation: ELEVATION,
  commonStyles,
  ...colorTokens,
};
export default theme;
