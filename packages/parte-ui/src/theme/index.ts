import {
  colorTokens,
  commonStyles,
  ELEVATION,
  typography,
  SPACING,
  COLORS,
} from '@parte/foundation';

const theme = {
  colors: COLORS,
  spacing: SPACING,
  elevation: ELEVATION,
  typography,
  commonStyles,
  ...colorTokens,
};

export default theme;
